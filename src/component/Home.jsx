import React from 'react';
import {Button} from "react-bootstrap";

const Home = () => {
    return (
        <div id='front-btn'>
            <Button variant="light" href='/list'>Let`s go for our product!</Button>
        </div>
    );
};

export default Home;